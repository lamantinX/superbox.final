{
  "name": "superbox",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run build -w shared && concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run build -w shared && npm run lint -w shared && npm run lint -w backend && npm run lint -w frontend",
    "test": "npm run build -w shared && npm run test -w shared && npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
